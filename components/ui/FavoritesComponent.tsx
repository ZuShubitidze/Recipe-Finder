// "use client";

// import { useAuth } from "@/context/auth-context";
// import { Button } from "./button";
// import { handleToggleFavorite } from "@/app/actions/toggleFavorites";

// const FavoritesComponent = ({ isFavorite }: { isFavorite: boolean }) => {
//   const { user } = useAuth();

//   return (
//     <div>
//       {user && <Button onClick={handleToggleFavorite}>Add to favorites</Button>}
//     </div>
//   );
// };

// export default FavoritesComponent;
